export type Club = {
  id: number;
  campus: string;
  name: string;
  locate: string;
  description: string;
  activity: string;
  mainCategory: string;
  subCategory: string;
  logoImagePath: string;
};
